node_modules/
dist/
*.gen.ts
*.gen.json

.notes/

# Lockfiles — bun is canonical, prevent accidental drift
package-lock.json
yarn.lock
pnpm-lock.yaml

# Next.js build output (examples/next-smoke)
.next/
*.tsbuildinfo
