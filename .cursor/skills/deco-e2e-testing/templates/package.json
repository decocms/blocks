{
  "name": "{{SITE_NAME}}-e2e-tests",
  "version": "1.0.0",
  "description": "E2E Performance Tests for {{SITE_NAME}}",
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "HEADED=true npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "install-browsers": "npx playwright install chromium",
    "baseline:save": "npx tsx scripts/baseline.ts save",
    "baseline:compare": "npx tsx scripts/baseline.ts compare",
    "baseline:list": "npx tsx scripts/baseline.ts list"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "tsx": "^4.19.0"
  }
}
